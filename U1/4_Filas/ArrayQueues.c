#include "ArrayQueues.h"

int main()
{
  // struct array_queue *queue = initialize(2);

  // push(queue, 5);
  // push(queue, 9);
  // push(queue, 25);

  // print_elements(queue);

  // printf("%d\n", peek(queue));

  // print_elements(queue);

  // pop(queue);
  // pop(queue);

  // print_elements(queue);

  // printf("%d\n", empty(queue));

  // printf("%d\n", len(queue));

  // printf("%d\n", peek(queue));

  // pop(queue);

  // print_elements(queue);
  
  return 0;
}